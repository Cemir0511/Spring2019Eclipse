package day41;

public class Teacher {
	
	
	private String name;
	private String speciality;
	
	// getters and setters
	
	// getter method is just like any other method
	//it is specifically for getting the field value
	// and returning it from the method
	//it accept no parameters
	
	
	public String getName() {
		return name;
		
	}
	
	// setter is just like any other method
	// it's specifically for setting/updating the filed value
	// and return nothing
	// It accept one parameter so we can pass the new value
	
	public void setName(String newName) {
	     name = newName;
    // bu 1. instnace variable olan name    
	
	     
	     
	     
	     
	}

/*
 * package day41;

public class Teacher {

  private String name;
  private String specialty;
  // getters and setters

  // getter method is just like any other method
  // it's specifically for getting the fields value
  // and returning it from the method
  // it accepts no parameter

  public String getName() {
    return name;
  }

  // setter method is just like any other method
  // it's specifically for setting/upading the field value
  // it accepts one parameter so we can pass the new value
  // and return nothing
  public void setName(String newName) {
    name = newName;
  }

  // getter method is just like any other method
  // it's specifically for getting the fields value
  // and returning it from the method
  // it accepts no parameter

  public String getSpecialty() {
    return specialty;
  }

  // setter method is just like any other method
  // it's specifically for setting/upading the field value
  // it accepts one parameter so we can pass the new value
  // and return nothing
  public void setSpecialty(String newSpecialty) {
    name = newSpecialty;
  }

}

 * 
 */









}








